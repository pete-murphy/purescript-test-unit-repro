{
  "scripts": {
    "postinstall": "spago install && spago build",
    "test": "spago test --main Test.Unit.Mytest"
  },
  "devDependencies": {
    "purescript": "^0.15.12",
    "spago": "^0.21.0"
  }
}
